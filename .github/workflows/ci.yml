name: CI Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  # Main CI job that runs the actual workflow file
  main:
    name: Run Main Workflow
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          path: 'src'
          
      - name: Run workflow
        run: |
          echo "Running main workflow from .github/.workflow/"
          # This will be replaced with the actual workflow execution
          # once we confirm the structure is correct
          echo "Workflow execution would happen here"
          
          # For now, just show the contents of the workflow directory
          echo "Workflow files available:"
          ls -la .github/.workflow/
